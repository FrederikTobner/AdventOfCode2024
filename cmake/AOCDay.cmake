macro(aoc_day day)
    set(DAY ${day})
    add_subdirectory(src)
    add_subdirectory(lib)
    if(TRUE AND EXISTS ${CMAKE_CURRENT_SOURCE_DIR}/test)
        add_subdirectory(test)
    endif()
    if(BUILD_BENCHMARKS AND EXISTS ${CMAKE_CURRENT_SOURCE_DIR}/benchmark)
        add_subdirectory(benchmark)
    endif()
endmacro(aoc_day )
